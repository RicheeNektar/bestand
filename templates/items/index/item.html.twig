<li class="list-group-item">
    <div class="row">
        <div class="col-lg col-lg-1 d-flex justify-content-center align-items-center mb-3 mb-lg-0">
            <img
                height="64px"
                style="aspect-ratio: 1"
                src="{{ url('images.get', {id: item.image.id}) }}"
                alt="product image"
            />
        </div>
        <div class="col-lg mb-3 mb-lg-0">
            <div class="row">
                <span class="col badge bg-secondary-subtle">
                    {{ 'item.name' | trans }}
                </span>
            </div>
            <div class="row mb-3">
                <span class="col">
                    {{ item.name }}
                </span>
            </div>
            <div class="row">
                <span class="col badge bg-secondary-subtle">
                    {{ 'item.category' | trans }}
                </span>
                <span class="col badge bg-secondary-subtle">
                    {{ 'item.retailer' | trans }}
                </span>
            </div>
            <div class="row">
                <span class="col">
                    {{ item.category.name }}
                </span>
                <span class="col">
                    {% if item.retailer.name is defined %}
                        {{ item.retailer.name }}
                    {% endif %}
                </span>
            </div>
        </div>
        <div class="col-lg col-lg-3 mb-3 mb-lg-0">
            <div class="row">
                <div class="col">
                    <div class="row">
                        <span class="col badge bg-secondary-subtle">
                            {{ 'item.price' | trans }}
                        </span>
                    </div>
                    <div class="row text-end mb-3">
                        <span class="col">
                            {{ item.price|number_format(4, ',', '.') }}
                        </span>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col">
                    <div class="row">
                        <span class="col badge bg-secondary-subtle">
                            {{ 'item.quantity' | trans }}
                        </span>
                    </div>
                    <div class="row text-end">
                        <span class="col">
                            {{ item.quantity }}
                        </span>
                    </div>
                </div>
                <div class="col">
                    <div class="row">
                        <span class="col badge bg-secondary-subtle">
                            {{ 'item.quantity.diff' | trans }}
                        </span>
                    </div>
                    <div class="row text-end">
                        <input
                            class="col form-control"
                            name="quantity[{{ item.id }}]"
                            type="number"
                            step="1"
                            value="0"
                            required
                        />
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg col-lg-1 d-flex justify-content-center align-items-center">
            <div class="d-none d-lg-flex btn-group-vertical">
                <button class="btn btn-primary" type="button" x-edit="{{ item.id }}" onclick="editDescription(this, {{ item.id }})">
                    <i class="bi bi-file-earmark-text-fill"></i>
                </button>

                <a class="btn btn-secondary" href="{{ item.link }}" target="_blank">
                    <i class="bi bi-box-arrow-up-right"></i>
                </a>
            </div>
            <div class="d-flex d-lg-none btn-group">
                <button class="btn btn-primary" type="button" x-edit="{{ item.id }}" onclick="editDescription(this, {{ item.id }})">
                    <i class="bi bi-file-earmark-text-fill"></i>
                </button>

                <a class="btn btn-secondary" href="{{ item.link }}" target="_blank">
                    <i class="bi bi-box-arrow-up-right"></i>
                </a>
            </div>
        </div>
    </div>
</li>